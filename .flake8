[flake8]
# flake8 7.x does NOT read pyproject.toml natively; this .flake8 file is the canonical
# config so no extra plugin (flake8-pyproject) is required.
max-line-length = 100
extend-ignore = E203, W503, E501
exclude =
    .git,
    __pycache__,
    models,
    data,
    results,
    logs
